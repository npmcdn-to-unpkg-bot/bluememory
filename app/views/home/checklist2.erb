<%= render :partial => "/home/sidebar" %>
<body>
<div class="phone-box wrap push">
    <!-- nav -->
	<div class="navtop">
		<div class="nav-left">
			<a href="#menu" class="menu-link"><img src="/assets/left-top.gif" class="nav-img"/></a>
		</div>
		<div class="nav-name">
			<h5 class="pro-link">CHECKLIST</h5>
		</div>
		<div class="nav-right">
		    <a href="/home/index"><img src="/assets/go-home.gif" class="nav-img"/></a>
		</div>
		<div class="clearfix"></div>
	</div> 
	
	<!--리얼 바디-->
    <div style="min-height: 560px; min-width:400px;
         background-image:url(/assets/korea.png);
         background-repeat:no-repeat;
         background-position: center;
         background-color: white;
         margin-bottom: 200px;"
	     >
        
        <!--
        <h1>asdfj;dfsa;fdsa;lfdsa</h1>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar0" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar1" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar2" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar3" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar4" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>
        <div class="col-md-2 col-xs-2 col-sm-12" id="bar5" style="text-align:center; padding:15px 15px 15px 15px; font-weight: 500; color: #303130;"></div>

        <input type="hidden" id="check_overall" value="3">
        <input type="hidden" id="check_difficulty" value="4">
        <input type="hidden" id="check_grade" value="4">
        <input type="hidden" id="check_study" value="4">
        <input type="hidden" id="check_achievement" value="3">
        <input type="hidden" id="check_attendance" value="5">
        -->
        <div id="bar1"></div>
    </div>
        
    <a href="http://www.facebook.com/sharer.php?s= 100&amp;p[title]=Title here&amp;p[url]=http://example.com&amp;p[images][0]=http://upload.wikimedia.org/wikipedia/commons/5/55/Kaasmarkt2_close.jpg&amp;p[summary]=I love cheese" target="_blank">Test</a>
     
</div>
</body> 
	   
	   
	   
<script>

var bar = new ProgressBar.Circle('#container001', {
  color: '#ff0000',
  strokeWidth: 6,
  trailWidth: 6,
  duration: 2500,
  text: {
    autoStyleContainer: false
  },
  from: { color: '#ff0000', width: 1 },
  to: { color: '#ff0000', width: 4 },
  step: function(state, circle) {
    circle.path.setAttribute('stroke', state.color);
    circle.path.setAttribute('stroke-width', state.width);

    var value = Math.round(circle.value() * 100);
    if (value == 8) {
      circle.setText('');
    } else {
      circle.setText(value);
    }

  }
});



/*======================================================*/
 var circle0 = new ProgressBar.Circle('#overall', {
        color: '#ffcc00',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target0 = bar.value();
            value0 =  parseFloat( $( '#check_overall' ).val() )
            if ( (target0 * 5).toFixed(1) == value0 ){
                circle0.stop();
            }
        }
});
      
circle0.animate(1, function() {  //이건 건들면 안돼
       circle0.animate(0);
});
  
/*=======================================================*/ 
    var circle1 = new ProgressBar.Circle('#bar1', {
        color: '#000099',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target1 = bar.value();
            if ( (target1 * 5).toFixed(1) == 4.0 ){
                circle1.stop();
            }
        }
      });
      
      circle1.animate(1, function() {  //이건 건들면 안돼
           circle1.animate(0);
      });
      
/*=======================================================*/  

      var circle2 = new ProgressBar.Circle('#bar2', {
        color: '#0066cc',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target2 = bar.value();
            value2 =  parseFloat( $( '#check_grade' ).val() )
            if ( (target2 * 5).toFixed(1) == value2 ){
                circle2.stop();
            }
        }
      });
      
      circle2.animate(1, function() {  //이건 건들면 안돼
           circle2.animate(0);
      });
      
/*=======================================================*/   

       var circle3 = new ProgressBar.Circle('#bar3', {
        color: '#009999',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target3 = bar.value();
            value3 =  parseFloat( $( '#check_study' ).val() )
            if ( (target3 * 5).toFixed(1) == value3 ){
                circle3.stop();
            }
        }
      });
      
      circle3.animate(1, function() {  //이건 건들면 안돼
           circle3.animate(0);
      });
      
/*=======================================================*/  

       var circle4 = new ProgressBar.Circle('#bar4', {
        color: '#00a400',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target4 = bar.value();
            value4 =  parseFloat( $( '#check_achievement' ).val() )
            if ( (target4 * 5).toFixed(1) == value4 ){
                circle4.stop();
            }
        }
      });
    
      circle4.animate(1, function() {  //이건 건들면 안돼
           circle4.animate(0);
      });
      
/*=======================================================*/        

       var circle5 = new ProgressBar.Circle('#bar5', {
        color: '#005d00',
        strokeWidth: 6,
        trailWidth: 6,
        duration: 2500,

        step: function(state, bar) {
            bar.setText((bar.value() * 5).toFixed(1));
            target5 = bar.value();
            value5 =  parseFloat( $( '#check_attendance' ).val() )
            if ( (target5 * 5).toFixed(1) == value5 ){
                circle5.stop();
            }
        }
      });
      
      circle5.animate(1, function() {  //이건 건들면 안돼
           circle5.animate(0);
      });
</script>